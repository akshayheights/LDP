resources:
  pipelines:
    sc_pipeline_1_pipeline:
      name: sc_pipeline_1_pipeline
      serverless: true
      channel: "PREVIEW"
      catalog: ${var.catalog}
      schema: ${var.schema}
      root_path: "."
      libraries:
        - glob:
            include: transformations/**
